# **📐 Gocator® 传感器选型指南**

![Version](https://img.shields.io/badge/VERSION-GoPxL_1.4-0078D7?style=for-the-badge)
![Author](https://img.shields.io/badge/AUTHOR-Alfred Pu-6f42c1?style=for-the-badge)

??? quote "📝 手册更新日志 (Changelog)"
    
    | 说明书版本 | 更新日期 | 更新者 | 更新说明 |
    | :--- | :--- | :--- | :--- |
    | **V0.8** | <code style="white-space: nowrap;">2026-04-01</code> | <span style="white-space: nowrap;">**Alfred&nbsp;Pu**</span> | 完成纯文字版本，还需要截图 |

选择合适的 Gocator 3D 智能传感器是确保项目成功的基石。Gocator 传感器作为高度集成的组件，出厂前已完成严苛的预校准，开箱即可输出以毫米 (mm) 为单位的高精度 3D 物理数据。

传感器的型号（如 2320、2520、5504）通常包含了关于该设备技术路线、激光等级/颜色以及硬件规格的重要信息。本章将带您深入了解传感器的核心参数，并引导您如何根据实际的现场需求选择最合适的型号。

---

## **1. 核心参数解析 (Core Parameters)**

在查阅 Gocator 硬件规格表时，您会经常遇到以下几个核心的三维成像参数。深刻理解它们是正确选型的前提。

### **1.1 空间视野与范围**
* **测量范围 (Measurement Range, MR)**：传感器在 Z 轴（深度/高度）方向上能够有效成像的有效范围。物体的高低起伏必须落在这个区间内。
* **视野 (Field of View, X FOV)**：传感器在 X 轴方向上能覆盖的宽度。注意，线激光传感器的视野是梯形的，这意味着它在近端（Near FOV）的视野较窄，而在远端（Far FOV）的视野较宽。
* **安装净距离 (Clearance Distance, CD)**：指传感器底部窗口到“测量范围”起点的物理绝对距离。对于小视野传感器，适度增大 CD 可以避免与现场机构发生干涉；但对于中大视野传感器，CD 过大不仅会导致传感器体积增加，还会降低全反射/镜面条件下的测量效果（综合考量建议适中，如 70mm 左右的平衡点）。

### **1.2 分辨率与精度**
* **Z 轴分辨率与重复性**：决定了您能测出多微小的厚度变化或划痕。Z 轴分辨率受限于电子噪声、环境变化，如果处于运动扫描中，还会受到“激光散斑效应 (Laser Speckle Effect)”的限制。
* **X 轴分辨率 (X Resolution / Spacing)**：激光线上相邻两个数据点之间的物理间距。由于视野是梯形的，近端 (Near End) 的 X 轴分辨率总是高于远端 (Far End)。当对分辨率要求极高时，应尽量将目标物放置在测量范围的近端。
* **Z 向线性度 (Z Linearity)**：测量绝对精度的关键指标。它表示在整个测量范围内，实际距离与传感器测量距离之间的最大偏差百分比。

### **1.3 扫描速度与处理性能**
* **最大帧率 (Maximum Frame Rate)**：传感器每秒可采集的最高 3D 轮廓数 (Hz)。帧率受限于 Z 轴的工作区域（Active Area）。**小贴士**：在软件中通过限制 Z 轴的搜索窗口（缩小 MR），可成倍提升扫描频率。
* **编码器与通讯延迟**：单台传感器直接接入编码器时，支持高达 1 MHz 的位置更新频率；通过 Master 控制器组网时，最大支持频率为 300 kHz。单传感器独立运行时的触发延迟仅为 8 微秒。

---

## **2. 需求导向：如何选择您的传感器？**

我们建议通过回答以下 6 个核心问题，层层递进地锁定您的最佳传感器：

### **🔍 问题 1：被测物体的材质是什么？**
材质决定了核心技术路线的选择：
* **普通非透明材质**（塑料、木材、哑光金属等）：标准的 **红光激光** 轮廓传感器（如 2300 系列）性价比最高。
* **高反光或精密机加工件**：由于蓝光波长短、不易穿透表面，能在光洁表面形成更锐利的激光线，建议选择 **蓝色激光** 型号（如 2400 / 2500 系列）。
* **透明、半透明或多层材料**（如玻璃、屏幕、胶路、晶圆）：必须选择 **3D 线共焦传感器 (Line Confocal)**。例如 Gocator 4000 或 5500 系列，它们基于色散共焦原理，甚至可以同时解析多达 8 层的内部结构。

### **🔍 问题 2：被测物体处于运动还是静止状态？**
* **运动目标**：选择 **线激光或线共焦传感器 (Line Profiler)**，配合传送带和编码器连续捕捉轮廓。
* **静止目标**：如果目标物固定（如机器臂抓取定位），选择 **3D 双目快照式传感器 (Snapshot)**，一次拍摄即可生成完整点云，无需相对运动。

### **🔍 问题 3：我要测量的物体有多大？**
* 确保选择的传感器 **X FOV 大于物体的最大宽度**，并留有物体摆放偏差的余量。
* **超大物体解决方案**：如果单台 FOV 无法覆盖，Gocator 支持多传感器智能组网（最多支持 24 台），可以将多台传感器配置为并排、多角度或 360° 环形布局，在软件中无缝拼接成一个超大点云。

### **🔍 问题 4：我需要检测多小的缺陷或特征？**
* **宏观检测**：测量纸箱体积或木材轮廓，可选择 2100 基础系列或视野达 2 米的 2600 系列。
* **微观精密检测**：检测消费电子引脚或芯片。推荐使用 2400 系列（200 万像素）或 2600 系列（4K+ 超高分辨率）。

!!! tip "选型经验公式"
    根据业界经验，通常建议：
    1. 所选传感器的 **Z 向重复性 ≤ 客户总公差的 1/50**。
    2. 传感器的 **Z/X 分辨率 应至少为您要检测的最小特征尺寸的 1/10 到 1/4**。

### **🔍 问题 5：生产线的运动速度有多快？**
计算沿传送带移动方向的采样间距（Y Spacing）：
* **公式**：所需扫描帧率 (Hz) = 传送带速度 (mm/s) / 所需的 Y 轴间距 (mm)。
* 如果您需要极高的在线扫描速度，**Gocator 2500 系列** 超高速传感器是理想选择（最高可达 20 kHz）。

### **🔍 问题 6：现场的安装空间是否受限？**
确认安装支架的高度是否会与生产线上的机械手、保护罩发生干涉。小视野传感器通常 **安装净距 (CD)** 较小，选型时必须对照参数表仔细评估物理干涉风险。

---

## **3. 选型总结与资源获取**

通过以上六步的维度筛查，您应该已经锁定了最佳的技术路线（激光/共焦/快照）及 1-2 款目标型号。

!!! info "下一步建议：验证与测试"
    1.  **查阅详细规格**：请查阅用户手册末尾的参数表，核对 Z 向线性度百分比、不同测量范围下的频率曲线图等。
    2.  **申请样机测试**：由于实际的 Z 轴精度会受目标物表面反光特性的显著影响，建议联系 **LMI Technologies** 官方销售或授权代理商，申请样机对真实材质进行实测。

---

<div align="center" style="margin-top: 30px; margin-bottom: 20px;">
  <img src="images/sensor/lmi_qrcode.png" alt="LMI Technologies 官方公众号" style="width: 180px; border-radius: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.12);" />
  <p style="color: #666; font-size: 14px; margin-top: 10px;"><b>扫码关注 LMI Technologies 官方微信</b><br><span style="font-size: 12px; color: #888;">获取最新技术白皮书、行业应用案例与样机申请</span></p>
</div>
